import React from 'react'
import { View, StyleSheet } from 'react-native'

const ParentBook = props => {
    return(
        <View></View>
    )
}

const s = StyleSheet.create({
    container: {
        flex: 1
    }
})

export default ParentBook